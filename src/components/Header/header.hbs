<header class="app-header">
    <div class="header-content">
        
        <div class="header-right">
            {{#if isAuthenticated}}
                <span class="welcome-text" id="userEmailBtn">Привет, {{userName}}</span>
                
            {{/if}}
        </div>
    </div>
</header>
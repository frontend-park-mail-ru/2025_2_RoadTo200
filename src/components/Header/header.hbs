<header class="app-header">
    <div class="header-content">
        
        <div class="header-right">
            {{#if isAuthenticated}}
                <span class="welcome-text">Привет, {{userName}}!</span>
                <button id="logoutBtn" class="logout-btn">
                    <span>Выйти</span>
                </button>
            {{/if}}
        </div>
    </div>
</header>